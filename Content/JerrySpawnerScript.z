class JerrySpawnerScript : ZilchComponent
{
    var Object : Cog;
    function Initialize(init : CogInitializer)
    {
        Zero.Connect(this.Space, Events.LogicUpdate, this.OnLogicUpdate);
    }

    function OnLogicUpdate(event : UpdateEvent)
    {
        Console.WriteLine(this.GameSession.GameStats.CurrentObject);
        if(this.GameSession.GameStats.CurrentObject == "Box2")
        {
            if(this.Object == null)
            {
                this.Object = this.Space.CreateAtPosition(Archetype.TrashCan, Real3(0.0, 7.0, 0.0));
            }
        }
    }
}
